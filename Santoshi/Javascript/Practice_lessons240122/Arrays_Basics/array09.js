"use strict";

/**
Complete the function sumGrades such that it returns the sum of all the grades
it receives as a parameter.
//
function logUserIds(array) {
  return array.forEach(function (userIds) {
    console.log(userIds);
  });
}
//
/**
 * @param {number[]} grades
 */

// function sumGrades(grades) {
//   return grades.(function (grades) {
//     console.log(grades);
//   });
// }

function sumGrades(grades) {
  let sum = 0;
  grades.forEach(function () {});
}

// Sample usage - do not modify
console.log(sumGrades([15, 5, 10])); // 30
console.log(sumGrades([12, 10, 13, 19])); // 54
