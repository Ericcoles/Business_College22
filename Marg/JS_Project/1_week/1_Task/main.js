alert("Your in Helsinki, Finland");

// git add .
// git commit - m 'insert meaning full message'(m meaning message)
//git remote add origin https://github.com/Ericcoles/REACT22K_JS.git (done only 1 time url is from github repository) (also make sure to use https and not ssh with git remote command, because we do not have full access to computer rights on the school macbooks)
// git remote -v (shows where repository is connected to)
// git push origin master
//option command ç

// M means modified. U means untracked.
